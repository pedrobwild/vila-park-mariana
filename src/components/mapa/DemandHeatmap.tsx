// Componente descontinuado: o mapa de calor de demanda de short-stay não se
// aplica ao Vila Park (empreendimento residencial). Mantido apenas por
// compatibilidade de import; não renderiza nada.
export default function DemandHeatmap() {
  return null;
}
